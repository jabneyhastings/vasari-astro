---
import SocialLinks from "../components/SocialLinks.astro";
import { Image } from 'astro:assets';
import { createUrl } from '../utils/url.ts';
import emblem from '../images/emblem.png';

const socialLinks = [
  { name: "Instagram", url: "https://instagram.com/vasaridesign_" },
  // { name: "X", url: "https://x.com/vasaridesign" },
  // { name: "Behance", url: "https://behance.net/vasaridesign" },
];
---

<footer class="footer">
    <div class="footer-content">
        <div class="footer-section">
            <Image src={emblem} alt="Vasari Logo" width={55} height={56} class="footer-title" />
        </div>
        <div class="footer-section footer-contact">
            <table>
                <tr>
                    <td class="bold">London:</td>
                    <td>
                        <a href="tel:+447903139144">+44 7903 139 144</a>
                        <a href="mailto:leigh@vasaridesign.co.uk">leigh@vasaridesign.co.uk</a>
                    </td>
                </tr>
                <tr>
                    <td class="bold">Dallas:</td>
                    <td>
                        <a href="tel:+18172715844">+1 817 271 5844</a>
                        <a href="mailto:roman@vasaridesign.com">roman@vasaridesign.com</a>
                    </td>
                </tr>
            </table>
        </div>

        <div class="footer-section footer-follow">
            <SocialLinks links={socialLinks} />
            <a href={createUrl('projects')} class="text-link">Projects</a>
            <a href={createUrl('contact')} class="text-link">Contact</a>
            <a href={createUrl('careers')} class="text-link">Careers</a>
        </div>
    </div>

    <div class="footer__copyright">
        <p>&copy; {new Date().getFullYear()} Vasari. All rights reserved.</p>
    </div>
</footer>

<style>
    .footer {
        width: 100%;
        padding: 40px 2rem 0 2rem;
        font-size: var(--fs-small);
        background: #cec7bb;
    }

    .footer-title {
        margin: 0 0 1rem 0;
    }

    .footer-content {
        padding: 40px 40px 80px 40px;
        max-width: var(--s-page-width);
        margin: 0 auto;
        display: grid;
        gap: 3rem;
        grid-template-columns: 2fr 1fr 1fr;
        grid-template-areas: ". contact follow";
        align-items: start;
    }

    .footer-contact { grid-area: contact; }
    .footer-follow  { grid-area: follow; }
    :global(.footer-follow a) {
        display: block;
        margin-bottom: 0.5rem;
    }

    .footer-section p,
    .footer__copyright p {
        line-height: 1.4;
        font-size: var(--fs-small);
    }

    .footer__copyright {
        text-align: center;
        padding: 0 0 50px 0;
    }

    @media (max-width: 1024px) {
        .footer-content {
            grid-template-columns: 1fr 1fr;
            grid-template-areas: "contact follow";
            gap: 2.5rem;
        }
    }

    @media (max-width: 768px) {
        .footer-content {
            grid-template-columns: 1fr;
            grid-template-areas:
                "contact"
                "follow";
            gap: 2rem;
            padding-bottom: 40px;
        }
    }
</style>