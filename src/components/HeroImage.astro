---
import CmsImage from '../components/CmsImage.astro';

const { src, alt } = Astro.props;
---

<section class="hero-image header__spacer" transition:name="hero">
    <CmsImage 
        src={src}
        alt={alt}
        width={2400}
        height={1350}
        loading={'eager'}
        class="hero-image__img"
        sizes="100vw"
    />
</section>

<style>
.hero-image {
    width: 100vw;
    max-width: 100%;
    max-height: 400px;
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
}

.hero-image__img {
    width: 100%;
    height: 400px;
    object-fit: cover;
    object-position: center;
    display: block;
}
</style>